# string-alignment
Using dynamic programming to best fit strings. Two strings are given, the first longer than the second. The goal is to find where to add spaces ("-") to match them the best.
Using a table that determines best fit the program finds the best path through the table.
